# JBM Electrical — Decap CMS configuration

# Backend — GitHub OAuth via our own Vercel functions
backend:
  name: github
  repo: framelywebdesigns/JBMElectricals
  branch: main
  base_url: https://jbm-electricals.vercel.app
  auth_endpoint: /api/auth

# Where uploaded media goes
media_folder: "assets/uploads"
public_folder: "/assets/uploads"

# Site URL
site_url: https://jbm-electricals.vercel.app
display_url: https://jbm-electricals.vercel.app
logo_url: /assets/images/jbm-logo.svg

# Content collections
collections:

  # ─── Gallery ─────────────────────────────────────────────
  - name: "gallery"
    label: "Gallery"
    label_singular: "Photo"
    folder: "content/gallery"
    create: true
    delete: true
    slug: "{{slug}}"
    extension: "json"
    format: "json"
    summary: "{{title}} ({{category}})"
    fields:
      - {label: "Title", name: "title", widget: "string", hint: "A short description of the photo"}
      - {label: "Category", name: "category", widget: "select", options: ["Domestic", "Commercial", "Industrial", "Rural"]}
      - {label: "Photo", name: "image", widget: "image", media_library: {config: {multiple: false}}}
      - {label: "Date", name: "date", widget: "datetime", default: "", format: "YYYY-MM-DD"}

  # ─── Blog ────────────────────────────────────────────────
  - name: "blog"
    label: "Blog"
    label_singular: "Post"
    folder: "content/blog"
    create: true
    delete: true
    slug: "{{slug}}"
    extension: "md"
    format: "frontmatter"
    summary: "{{title}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Cover Image", name: "cover", widget: "image"}
      - {label: "Date", name: "date", widget: "datetime", default: "", format: "YYYY-MM-DD"}
      - {label: "Excerpt", name: "excerpt", widget: "text", hint: "A short summary shown on the blog listing — 1-2 sentences"}
      - {label: "Body", name: "body", widget: "markdown"}
